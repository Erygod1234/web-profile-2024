import React from "react";
import Card from "./common/Card";

type Props = {};

const Bio = (props: Props) => {
  return (
    <Card title="Bio">
      <div>
      I want to practice writing font ends and back ends in order to become a full stack in the future.
      I love programming. So I have the encouragement to write various code.
      </div>
    </Card>
  );
};

export default Bio;
